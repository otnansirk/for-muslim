services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    ports:
      - "1323:1323"
    volumes:
      - ./backend:/app
    environment:
      - GO111MODULE=on
      - API_KEY=PASTE_API_KEY
      - PRAYER_BASEURL=PASTE_PRAYER_BASEURL
    command: air -c .air.toml
